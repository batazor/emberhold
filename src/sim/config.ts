/**
 * Числа из DESIGN §11. Всё, что там не зафиксировано, помечено как placeholder —
 * такие значения настраиваются телеметрией, а не спором.
 */
import type { Tier } from './types';

/**
 * Числа ярусов больше не назначаются здесь — они выводятся моделью (§22)
 * из описания сложности и переэкспортируются, чтобы существующие импорты
 * не менялись. Править ярус означает править TIER_SPEC в balance.ts,
 * а не четвёрку чисел, подогнанных друг под друга.
 */
export {
  FOOD_COST,
  HERO_WOUNDS,
  TIER_CONTAINERS,
  TIER_CONTAINER_BASE,
  TIER_DEPTH_VALUE,
  TIER_FOOD,
  TIER_KITCHEN_GATE,
  TIER_RISK,
  TIER_SIZE,
  modelKitchenFood,
} from './balance';

export const TIER_NAME: Record<Tier, string> = {
  0: 'Подступы',
  1: 'Ярус 1',
  2: 'Ярус 2',
  3: 'Дно',
};

/** §11.4 — радиус обзора в тайлах. */
export const visionRadius = (knowledge: number, night: boolean, torch: boolean): number =>
  3 + Math.floor(knowledge / 5) + (night ? -1 : 0) + (torch ? 2 : 0);

/**
 * Доля радиуса света, на которой противник просыпается. Меньше единицы —
 * значит игрок видит врага раньше, чем враг видит его, и у прохода через
 * комнату появляется решение: обойти, проскочить или драться (§15).
 * При единице все, кто попал в свет, кидаются разом, и решения нет.
 */
export const ENEMY_WAKE_SHARE = 0.6;

/**
 * §11.3 — сколько очков стойкости противника стоит одна рана по старой шкале.
 *
 * Здоровье врага игрок числом не видит и не видел никогда, поэтому там шкала
 * может быть непрерывной, не нарушая правила «раны целые». Величина заведена
 * эталоном, а не украшением: Атака героя калибруется относительно неё —
 * четвёрка бьёт как прежний удар, тройка медленнее, пятёрка быстрее.
 *
 * Менять её означает менять цену каждого противника разом, поэтому она одна
 * и лежит здесь, а не размазана по ENEMY_STATS.
 */
export const HP_PER_WOUND = 4;

/**
 * Урон героя, пока Атака в бой не входит. Равен эталону намеренно: замена
 * шкалы обязана быть рефактором, а не правкой баланса, и золотой мастер
 * не имеет права сдвинуться от неё ни на цифру. Заменяется на loadout.attack
 * следующим шагом — вместе с перемером.
 */
export const HERO_ATTACK_REF = HP_PER_WOUND;

/**
 * §11.3 — потолок смягчения: какая доля удара проходит при любой Защите.
 *
 * Это то же `max(1, …)` из прежней формулы, записанное долей вместо единицы.
 * Смысл тот же и он важнее удобства: **неуязвимости не существует
 * по построению, а не по настройке.** Без этой доли Рыцарь со щитом пятого
 * уровня перестаёт получать урон вовсе, и бой выбывает из причин провала
 * механически — то есть §11.3 ломается не числом, а конструкцией.
 */
export const MIN_PIERCE_SHARE = 0.25;

/**
 * §11.3 — сколько пробоя стоит вторая рана.
 *
 * Задаёт ярус, с которого Защита вообще видна: пока пробой не дорос,
 * все бьют по одной ране и Защита незаметна; глубже часть ударов стоит двух,
 * и Защита возвращает их к одной. Это и есть пересмотр «фонарь или щит перед
 * каждым ярусом», которого требует §14.2.
 *
 * Число черновое до перемера (§22.6).
 */
export const PIERCE_STEP = 6;

/** §11.3 — герой: раны переехали в balance.ts, там на них опирается модель. */
export const HERO_ATTACK_INTERVAL = 1.2;
export const HERO_REACH = 1.0;

/** §17.4 — закреплённая дизайнерская константа, не следствие анимации. */
export const HERO_SPEED = 1.67;

/** Вес добычи замедляет героя (§1). Доля скорости, теряемая на полном рюкзаке. */
export const WEIGHT_SLOWDOWN = 0.35;

/** Знание героя. Героев как сущностей ещё нет (§7 — этап 5), поэтому
 *  характеристика задана константой и питает формулу обзора из §11.4. */
export const HERO_KNOWLEDGE = 5;
